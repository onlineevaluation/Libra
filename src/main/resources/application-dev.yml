spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dev?autoReconnect=true&useSSL=false
    username: root
    password: 1234
  jpa:
    database: mysql
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL5Dialect
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.163.com
    username: youngxhui@163.com
    password: nwcoiphamivwxhdw
    default-encoding: UTF-8
    properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
              required: true

server:
  port: 8080

